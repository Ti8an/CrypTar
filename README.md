# 🧱 CrypTar

**CrypTar** — это простой Bash-инструмент для архивации и шифрования данных с помощью `tar` и `gpg`.

---

## 🔧 Установка

```bash
chmod +x install.sh
./install.sh
